package String_methodes;

public class intResult {
    public static void main(String[] args) {

        String name = "m1keus";

        int result = name.length();
        // name.length() в данном случае отображает количество букв в написанном слове

//      int result = name.indexOf("m");
        // Показывает позицию, на которой находится запрошенная буква или цифра
        // В данном чслучае покажет: 0

        System.out.println(result);
    }
}
