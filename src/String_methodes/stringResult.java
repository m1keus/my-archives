package String_methodes;

public class stringResult {
    public static void main(String[] args) {

        String name = "m1keus";

        String result = name.toUpperCase();
        // Заменяет все буквы в указанном слове на большие

//      String result = name.toLowerCase();
        // Заменяет все буквы в указанном слове на маленькие

//      String result = name.trim();
        // Убирает все пустое пространство, если оно есть

//      String result = name.replace('m', 'l');
        // Заменяет указанную букву на другую


        System.out.println(result);
    }
}
